## 金融仪表盘生成器

模板驱动的本地 HTML 金融仪表盘生成器，不执行任意代码，也不依赖外部沙箱服务。

```bash
cd 21-finance_dashboard_generator
pip install -r requirements.txt
streamlit run app.py
```

仓库根目录运行：

```bash
./scripts/run_21_agent.sh
```

> 本项目仅用于技术学习与原型验证。
