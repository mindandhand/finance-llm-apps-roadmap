## 市场事件 Radar Agent

监控关注列表中的公告、财报、监管、产品和重大新闻，生成 dry-run 事件摘要。

```bash
cd 19-market_event_radar_agent
pip install -r requirements.txt
streamlit run app.py
```

仓库根目录运行：

```bash
./scripts/run_19_agent.sh
```

> 本项目仅用于技术学习与原型验证，不构成投资建议。
